/**
 * Creator Flow v2 — the registers.
 *
 * `docs/phases/creator-flow-v2.md` is the brief;
 * `docs/phases/creator-flow-reconciliation.md` is the walk.
 *
 * Session A built no surface. What it left is the guarantee rather than the
 * content: the page register (empty until a session renders a page into it),
 * the refusals, the two records that close a §5.3 gap, and the four deviation
 * vocabularies — each carrying, beside the thing it permits, the sentence that
 * keeps it narrow.
 *
 * Session B added the four screens the invitation token opens, and the copy
 * that is load-bearing rather than decorative; Session C the rest of the walk
 * and the claim; Session D the app shell and Home, which is `app.ts`; Session F the work
 * surface, Earnings, Resources and Settings — `work.ts` and `money.ts`, over
 * registers `resources.ts` and `settings.ts` had been holding since Session A.
 * Session E, built last, is `pitches.ts`: the two modes, the reveal with its
 * keyboard path, and §14.1's own list as the recap's register.
 */
export * from './flow.js';
export * from './onboarding.js';
export * from './voice.js';
export * from './standing.js';
export * from './referrals.js';
export * from './resources.js';
export * from './channels.js';
export * from './settings.js';
export * from './app.js';
export * from './work.js';
export * from './money.js';
export * from './pitches.js';
