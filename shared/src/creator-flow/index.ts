/**
 * Creator Flow v2 — the registers.
 *
 * `docs/phases/creator-flow-v2.md` is the brief;
 * `docs/phases/creator-flow-reconciliation.md` is the walk.
 *
 * Session A builds no surface. What is here is the guarantee, not the content:
 * the page register (empty until Session B renders one), the refusals, the two
 * records that close a §5.3 gap, and the four deviation vocabularies — each
 * carrying, beside the thing it permits, the sentence that keeps it narrow.
 */
export * from './flow.js';
export * from './voice.js';
export * from './standing.js';
export * from './referrals.js';
export * from './resources.js';
export * from './channels.js';
export * from './settings.js';
